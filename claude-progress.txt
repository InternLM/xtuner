Session role: initializer / planning only.

What I accomplished:
- Read `zdev/zcoding/Agents.md` for the branch layout and current RL implementation anchors.
- Read `design/disagg_design.md` end to end and checked the draft pseudocode in `design/disagg_draft.py`.
- Inspected the current runtime files:
  - `xtuner/v1/rl/agent_loop/producer.py`
  - `xtuner/v1/rl/agent_loop/agent_loop_manager.py`
  - `xtuner/v1/rl/agent_loop/single_turn_agent_loop.py`
  - `xtuner/v1/rl/agent_loop/utils.py`
  - `examples/v1/config/rl_disagg_single.py`
  - `examples/v1/config/rl_disagg_multi.py`
- Confirmed that `xtuner/v1/train/rl_disaggregated_trainer.py` does not exist yet in this checkout, even though the draft configs already import it.
- Created and then updated `feature_list.json` as the source of truth for the implementation order and testing scope.
- The current planning state in `feature_list.json` has 9 features total, and the first producer feature is already marked `passes: true`.
- The plan now explicitly includes:
  - shared `ProduceStrategy` / `SyncProduceStrategy` interface alignment
  - `RLColocateTrainer` compatibility work in `xtuner/v1/train/rl_colocate_trainer.py`
  - colocate trainer integration regression coverage focused on `examples/v1/config/rl_grpo_gsm8k_judge.py` and `examples/v1/config/rl_multi_task_gsm8k_dapo_math.py`
  - `zdev/rl_design_colocate.sh` as the reference colocate launch flow for integration or smoke coverage design

Key findings for the next agent:
- The design is intended as a minimal refactor, not a rewrite: reuse `AgentLoopManager + AsyncProduceStrategy + ReplayBuffer`.
- The shared strategy interface matters now, not just the async strategy: `ProduceStrategy` base class and `SyncProduceStrategy` must stay aligned with the manager refactor so colocate and disaggregated paths do not diverge.
- The core runtime split is:
  - producer-only path: `_produce_batch_to_buffer(...)`
  - explicit stop/reap path: `cleanup_pending_tasks(...)`
  - consumer-only path: `_get_batch_from_buffer(...)`
- The most important semantic change is that rollout freshness must track `model_rollout_step`, not the producer loop's local iteration count.
- `RLColocateTrainer` is now an explicit compatibility target, not just something indirectly covered by shared tests.
- `EXPIRED_BATCH` is intentionally aggressive: once rollout weights are stale enough, skip training and prioritize weight sync immediately.
- Checkpoints must be taken only after producer cleanup and before `reset()`, with the saved rollout step overridden to the post-sync target version.
- Resume should restore the manager into a paused `UPDATE_ABORT` state, re-sync rollout weights from train, then call `reset()` and start a fresh producer task.
- Colocate regression coverage should focus first on:
  - `examples/v1/config/rl_grpo_gsm8k_judge.py`
  - `examples/v1/config/rl_multi_task_gsm8k_dapo_math.py`
  - launch wiring modeled after `zdev/rl_design_colocate.sh`

Suggested implementation order:
1. `xtuner/v1/rl/agent_loop/producer.py`
2. shared `ProduceStrategy` / `SyncProduceStrategy` contract alignment and affected producer call sites
3. `xtuner/v1/rl/agent_loop/agent_loop_manager.py`
4. `xtuner/v1/rl/agent_loop/single_turn_agent_loop.py` and `xtuner/v1/rl/agent_loop/utils.py`
5. `xtuner/v1/train/rl_colocate_trainer.py`
6. `xtuner/v1/train/rl_disaggregated_trainer.py`
7. checkpoint/resume details
8. colocate integration coverage centered on the two colocate configs above
9. disaggregated tests and example-config smoke coverage

Notes about the worktree:
- `git status --short` showed untracked `work_dirs/` and `zdev/` before this session.
- I did not touch those paths.

---

本次实现进展补充（保留以上原始 planning 内容）：

本次完成内容：
- 完成 `xtuner/v1/rl/agent_loop/producer.py` 的第一阶段重构，使 `AsyncProduceStrategy.produce_batch()` 改为返回 `ProduceBatchStatus`，并从“边生产边清理边取 batch”的旧逻辑，调整为“只负责向 replay buffer 生产数据”的新逻辑基础形态。
- 将异步 producer 的 pending rollout 任务从单次调用内的局部变量，提升为 `AsyncProduceStrategy._pending_tasks`，让未完成任务可以跨多次 `produce_batch()` 调用保留。
- 暴露公开接口 `cleanup_pending_tasks()`，把 pending rollout 的显式收尾从 `produce_batch()` 尾部自动 cleanup 中拆出来。
- 增加 `ProduceBatchStatus` 导出，补齐 `ProduceStrategy` / `SyncProduceStrategy` 与新接口的共享签名，避免共卡路径和后续非共卡路径接口分叉。
- 在 rollout group 上写入 `extra_fields["group_generate_time_s"]`，为后续 manager 消费阶段重建 timing 统计打基础。
- 在 `xtuner/v1/rl/agent_loop/agent_loop_manager.py` 中加入一个最小兼容层，使当前 colocate `produce_batch()` 仍能兼容新的 async producer 返回值，并在取回 completed groups 后重建 timing 统计。
- 扩展 `tests/rl/test_producer.py`，覆盖以下关键行为：
  - `NORMAL` 状态下跨调用回收 pending 任务
  - `cleanup_pending_tasks()` 的显式清理行为
  - `UPDATE_ABORT`
  - `EXPIRED_BATCH`
  - group generate timing 落盘到 `extra_fields`

本次涉及文件：
- `code_agent.md`
- `run_test.sh`
- `xtuner/v1/rl/agent_loop/producer.py`
- `xtuner/v1/rl/agent_loop/agent_loop_manager.py`
- `xtuner/v1/rl/agent_loop/__init__.py`
- `tests/rl/test_producer.py`

验证结果：
- 运行 `bash run_test.sh`
- 当前脚本内容会进入 `py312-pt28-raw` 环境，并执行：
  - `tests/rl/test_agent_loop.py`
  - `tests/rl/test_multi_task_agent_loop_manager.py`
  - `tests/rl/test_producer.py`
- 结果：`13 passed`

本次发现与说明：
- `claude-progress.txt` 已按要求仅追加，不删除原有 planning 内容。
- 当前工作区里未跟踪的 `work_dirs/` 与 `zdev/` 仍保持未纳入本次提交。

建议下一步：
1. 继续把 `AgentLoopManager` 按 `design/disagg_design.md` 拆成 `_produce_batch_to_buffer()` / `_get_batch_from_buffer()` / `produce_loop()` / `get_batch()` 的完整状态机实现。
2. 然后再推进 `single_turn_agent_loop.py` 与 `utils.py` 中 `model_rollout_step` / staleness 语义改造。

---

本次实现进展补充（共享 strategy 契约继续收口）：

本次完成内容：
- 按最新约定把 `ProduceStrategy.produce_batch()` 统一为只返回 `ProduceBatchStatus`，不再保留 `ProducerTimings` 这一条并行返回路径。
- 将 group generate timing 的落盘逻辑从 `_record_group_generate_time(...)` 内联到 `_timed_generate_group(...)`，保证样本一生成完成就带上 `extra_fields["group_generate_time_s"]`。
- 调整 `SyncProduceStrategy.produce_batch()`，使其在接受共享新签名 `rollout_step / model_rollout_step / update_event` 的同时，继续保持当前 colocate 语义。
- 调整 `AgentLoopManager` 的 strategy 调用点，显式按共享新签名传参，并统一按 replay buffer 中的 rollout group 重建 timing 统计。
- 去掉 manager 中对 `ProducerTimings | ProduceBatchStatus` 双轨返回的兼容分支，收敛为单一路径。
- 更新 `tests/rl/test_multi_task_agent_loop_manager.py`：
  - fake strategy 改为匹配统一的新签名
  - 新增 status-returning strategy 用例，验证 manager 会走 cleanup 并从 group 上重建 timing
- 更新 `tests/rl/test_producer.py`：
  - `SyncProduceStrategy` 测试改为显式通过共享新签名调用
  - 验证 sync / async `produce_batch()` 都返回 `ProduceBatchStatus.NORMAL`

本次涉及文件：
- `xtuner/v1/rl/agent_loop/producer.py`
- `xtuner/v1/rl/agent_loop/agent_loop_manager.py`
- `tests/rl/test_multi_task_agent_loop_manager.py`
- `tests/rl/test_producer.py`
- `feature_list.json`

验证结果：
- 运行 `bash run_test.sh`
  - 结果：`11 passed`
- 额外运行：
  - `pytest -q tests/rl/test_multi_task_agent_loop_manager.py tests/rl/test_producer.py tests/rl/test_agent_loop.py::TestAgentLoop::test_gsm8k_agent_loop_manager`
  - 结果：`12 passed`

本次发现与说明：
- 共享 strategy 契约现在已经从 base class、sync strategy、manager 调用点，到测试 fake strategy 一起收敛到了同一套接口。
- 目前 `run_test.sh` 仍保持你精简后的快测形态，没有把 `tests/rl/test_agent_loop.py` 整体加回去；我只在本次验证里额外单独跑了其中一个关键共卡 manager 用例。

建议下一步：
1. 继续做 `AgentLoopManager` 的完整 producer/consumer 状态机拆分，把当前兼容层推进为真正的 `_produce_batch_to_buffer()` / `_get_batch_from_buffer()` / `cleanup_pending_tasks()` / `produce_loop()` / `get_batch()`。
2. 然后推进 `single_turn_agent_loop.py` 与 `utils.py` 的 `model_rollout_step` / staleness 语义改造。
