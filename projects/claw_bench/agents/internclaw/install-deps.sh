#!/bin/bash
# internclaw agent install-deps stub — runner already mounts lagent source +
# /tmp/lagent-py wrapper; nothing agent-specific to install here.
set -euo pipefail
echo "[internclaw] install-deps: ok"
